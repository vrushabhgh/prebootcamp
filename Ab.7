const readline = require('readline');
const inp = readline.createInterface({input: process.stdin});
const userInput = [];
inp.on("line", (data) =>
{userInput.push(data);
    });
inp.on("close", () => {//
 var N = +userInput[0];
 var p=[];
if(N===0)
 {console.log("NULL");
}
else
{for(i=1; i<=N; i++)
{
    p.push(9*i)
 }
 console.log(p.join(" "));
}
});
